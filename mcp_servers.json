{
  "mcpServers": [
    {
      "name": "filesystem",
      "description": "Local filesystem read/write/search via official MCP server. Set enabled=true to activate (requires Node.js / npx).",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/"],
      "enabled": false
    },
    {
      "name": "github",
      "description": "GitHub PRs, issues, commits, and repo management",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}" },
      "enabled": false
    }
  ]
}
